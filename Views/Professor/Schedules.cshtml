@model IEnumerable<UM_Project.Models.Schedule>
@{
    ViewData["Title"] = Localizer["Nav_MySchedules"];
    ViewData["PageTitle"] = Localizer["Page_ProfessorSchedules"];
    ViewData["PageIcon"] = "fa-calendar-alt";
    ViewData["ShowCreate"] = false;
}

<div class="um-page">
    <partial name="_AdminPageHeader" />
    @if (!Model.Any())
    {
        <div class="role-empty"><i class="fas fa-calendar d-block"></i><p class="mb-0">@Localizer["Msg_NoSchedules"]</p></div>
    }
    else
    {
        <div class="um-panel">
            <div class="table-responsive">
                <table class="um-table">
                    <thead><tr><th>@Localizer["Label_Course"]</th><th>Day</th><th>Time</th><th>Room</th></tr></thead>
                    <tbody>
                        @foreach (var s in Model)
                        {
                            <tr>
                                <td class="fw-semibold">@s.Course?.CourseName</td>
                                <td>@s.Day</td>
                                <td>@s.Time</td>
                                <td>@s.Room</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    }
    <a asp-action="Dashboard" class="um-btn-primary mt-3"><i class="fas fa-arrow-left"></i> Dashboard</a>
</div>
